<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Main Page</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1 >Quora</h1>
    <% for (let post of posts) {%>
        <div class="box">
            <h3 id="user">@<%= post.username %></h3>
            <h4 id="content"><%= post.content %></h4>
            <a href="http://localhost:8080/post/<%=post.id %>">See post in detail.</a>
             <a href="http://localhost:8080/post/<%=post.id %>/Edit">Edit</a>
             <form method="post" action="/post/<%= post.id %>?_method=Delete">
                <button>Delete Post</button>
             </form>
        </div>
    <% } %>
    <br>
    <br>
    <a href="http://localhost:8080/post/new">Create new Post</a>
</body>
</html>